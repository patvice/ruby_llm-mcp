#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

npm --prefix "$ROOT_DIR/test_server" install
bundle install --gemfile "$ROOT_DIR/rails_app/Gemfile"

echo "Setup complete."
echo "Run the app with: cd $ROOT_DIR/rails_app && bin/rails server"
